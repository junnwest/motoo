/**
 * 고객센터 contact channel. A personal inbox for now (owner's own email) — the
 * product doesn't have a real support desk yet. `/refund` and the footer both
 * point here; swap this one constant when a real channel exists.
 */
export const SUPPORT_EMAIL = "junn223@gmail.com";
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
