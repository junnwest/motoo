import { ShellSkeleton } from "@/components/ShellSkeleton";

/** Streams instantly while the page's server work runs. See ShellSkeleton. */
export default function Loading() {
  return <ShellSkeleton />;
}
